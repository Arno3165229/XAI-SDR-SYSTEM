version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - XAI_API_KEY=${XAI_API_KEY}
    volumes:
      - .:/app
      - /app/sdr_database.db